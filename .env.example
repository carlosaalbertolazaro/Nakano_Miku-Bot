# Copiá este archivo a .env y completá los valores reales. Nunca subas .env al repo.

NODE_ENV=production

# --- Módulo de IA (Gemini) — se construye en una fase futura, podés dejarlo vacío por ahora ---
GEMINI_API_KEY=

# --- Alertas de servidor Aternos (Minecraft) — se construye en una fase futura ---
ATERNOS_SERVER_HOST=
ATERNOS_SERVER_PORT=
